<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dahaedream.message.mapper.MessageMapper">
    <select id="selectMemberByNickname" parameterType="string" resultType="com.dahaedream.login.model.MemberDto">
        SELECT member_id, email, nickname FROM member WHERE nickname = #{nickname}
    </select>

    <insert id="insertMessage" parameterType="com.dahaedream.message.model.MessageDto">

    </insert>
</mapper>